#!/usr/bin/env perl

use strict;
use warnings;
use App::AltSQL;

my $c = App::AltSQL->new_from_cli();
#$c->view->load_plugin($_) foreach qw(Color UnicodeBox);
$c->load_plugin($_) foreach qw(Tail);
$c->run();
